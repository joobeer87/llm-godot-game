# LLM Game Project

This repository contains a minimal Godot 4 project scaffold for iterative game development with a language model.

Use the prompts in the `prompts/` folder to guide an LLM agent to design, implement, test and balance the game.  
The CI workflow will export a Web build automatically on every commit via GitHub Actions.
